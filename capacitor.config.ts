import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.peneira.app",
  appName: "Peneira",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
